apiVersion: v1
kind: Service
metadata:
  name: {{ include "dodo-helm-tutorial.fullname" . }}
  labels:
    {{- include "dodo-helm-tutorial.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dodo-helm-tutorial.selectorLabels" . | nindent 4 }}
