management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  application:
    name: foodservice
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
  jackson:
    serialization:
      fail-on-empty-beans: false
  datasource:
    initialization-mode: always